# v0.129.0

This release merges upstream OpenAI Codex `rust-v0.129.0` into the Android Termux fork and publishes it as `@mmmbuto/codex-cli-termux@0.129.0`.

## Upstream base

- OpenAI Codex `rust-v0.129.0`

## Termux fork changes

- Android ARM64 native package for Termux.
- `codex` and `codex-exec` packaged together.
- Browser login routed through `termux-open-url`.
- Fork self-update points to `DioNanos/codex-termux` and `@mmmbuto/codex-cli-termux`.
- Packaged launchers preserve `CODEX_SELF_EXE` and bundled `libc++_shared.so` visibility.
- Android ELFs use `RUNPATH=$ORIGIN` for direct native invocation.
- Voice and realtime audio remain disabled in the published Termux package.
- Maintainer builds use fork-owned Android `rusty_v8` artifacts.

## Verification

- Termux patch inventory: required before publish.
- Android ARM64 release build: required before publish.
- npm package `@mmmbuto/codex-cli-termux@0.129.0`: published with `latest`.
- Manual Termux device test report will be added after release.
