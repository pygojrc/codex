name: Legacy Termux release workflow disabled

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - name: Use version-specific workflow
        run: |
          echo "This legacy workflow is disabled."
          echo "Use .github/workflows/termux-release-v0.144.6.yml."
